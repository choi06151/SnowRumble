# Task C-39 - UE5 코드 컨벤션 정리

## 설명

C 소유 Unreal C++ 코드를 UE5에서 일반적으로 사용하는 읽기 쉽고 일관된 컨벤션으로 정리한다. 게임 동작, 공용 API, RPC, 복제 계약과 Blueprint 연결을 유지한다.

## 상태 전이 기준

- 시작 가능: 사용자 구현 승인, C 브랜치 동기화 완료
- 완료 가능: C 소유 코드 정리, 정적 점검과 빌드 검증, 타 담당자 파일의 후속 인계 기록

## 구현 항목

- [x] C 소유 헤더·구현 파일에 공통 포맷 기준을 추가하고 선언 순서, 접근 지정자와 Unreal 매크로 배치 기준을 명시한다.
- [x] UE5 표준 타입·포인터·const/reference·bool 변수 명명 규칙을 동작 보존 범위에서 점검하고 기존 일관성을 확인한다.
- [x] 함수 역할이 코드만으로 드러나지 않는 C 소유 함수에 한글 주석을 보완한다.
- [x] K/J 소유 및 소유자 미표기 파일은 수정하지 않고 후속 인계 범위로 기록한다.

## 작업 배정

- 담당자: 최재원(C)
- 기능 소유자: 최재원(C)
- 계약 소유자: 최재원(C)
- 자산 수정자: 없음
- 생성·변경 파일: `.clang-format`, C 소유 `Source/SnowRumble/` 아래 83개 `.h`·`.cpp` 파일, 본 Task와 `Tasks/C/PLAN_C.md`
- 공유 확인 대상: K/J 소유 및 소유자 미표기 C++ 파일
- 병합 순서: C 브랜치 검증 후 역할 브랜치 게시. 공용 계약 변경이 없으므로 별도 선행 Task 없음

## 공용 계약과 인계

- 제공받을 계약: 없음
- 제공할 계약: 기존 공용 API·이벤트·복제 상태를 그대로 유지
- 인계 대상: K/J 담당자와 공용 코드 소유자

## 범위 밖

- 다른 담당자 소유 파일의 직접 수정
- Unreal Blueprint·맵·uasset·umap 수정
- 함수 시그니처, RPC, 복제 프로퍼티와 게임 규칙 변경
- 새 포맷터·플러그인·모듈 도입

## 사전 전제

- `docs/STYLEGUIDE.md`의 C++·캡슐화·멀티플레이 기준
- C++ 위주 개발 스타일

## 결정 필요

- 없음. 포맷·저작권 헤더 정리는 에디터 자산 작업이 아니다.

## 변경 기록

- 2026-09-14: 사용자 요청과 승인으로 C 소유 코드 컨벤션 정리 Task를 추가했다. 다른 담당자 파일은 인계 대상으로 분리한다.
- 2026-09-14: C 소유 83개 파일에 `.clang-format`을 적용하고 함수 역할이 불명확한 구간의 주석을 보완했다. 동작·공용 계약 변경 없이 정적 점검과 Unreal Editor Development 빌드를 통과했다.

## 수동 작업

- 없음

## 완료 조건

### 에이전트 확인

- [x] 관련 C 소유 C++ 코드 변경 완료
- [x] 로컬 정적 점검 및 프로젝트 빌드 확인 완료 — `clang-format --dry-run --Werror`, `git diff --check`, `SnowRumbleEditor Win64 Development` 성공
- [x] 역할·소유권·담당자 이니셜 규칙 위반 없음
- [x] 공용 계약과 캡슐화 규칙 위반 없음
- [x] 다른 담당자 파일의 후속 인계 내용 기록 완료
- [x] 현재 Task 문서가 실제 구현 기준으로 갱신됨

### 결과 확인

- [x] C 소유 코드가 기존 동작과 공용 계약을 유지하는지 확인 — 포맷 변경만 적용했고 UHT·Unreal Editor Development 빌드를 통과했다.
- [x] Unreal Editor Development 빌드가 성공하는지 확인 — 2026-09-14, UnrealBuildTool `Result: Succeeded`
